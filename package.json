{
  "name": "beatass",
  "version": "0.1.0",
  "private": true,
  "description": "beatass.com prototype — anonymous confession + voodoo doll GIF",
  "scripts": {
    "build": "python3 build.py",
    "test": "python3 build.py && node test-relay.mjs && node tools/instagram/notify.mjs --selftest && node tools/instagram/outreach.mjs --selftest && node tools/classify.mjs --selftest && node test.mjs && node test-queue.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "playwright": "^1.56.0",
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "postal-mime": "^2.7.5"
  }
}
