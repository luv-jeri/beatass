{
  "name": "beatass",
  "version": "0.1.0",
  "private": true,
  "description": "beatass.com prototype — anonymous confession + voodoo doll GIF",
  "scripts": {
    "build": "python3 build.py",
    "test": "python3 build.py && node test.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "playwright": "^1.56.0",
    "wrangler": "^4.118.0"
  }
}
